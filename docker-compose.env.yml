version: "3"
services:
  db:
    image: "mongo"
    restart: always
    container_name: "authService"
    ports:
      - 27017:27017
  authservice:
    build: .
    ports:
      - 8080:3005
    volumes:
      - "/home/authService/node_modules"
      - ".:/home/authService"
    links:
      - db
    environment:
      - NODE_ENV=development
